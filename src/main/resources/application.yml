server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/db_tracker
    username: root
    password: root
    driver-class-name: com.mysql.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update